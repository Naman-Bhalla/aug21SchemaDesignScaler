spring.datasource.url=jdbc:postgresql://localhost:5432/aug21
spring.datasource.username=aug21
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.port=8081

# create -> only create new tables/ changes./  Don't remove anything that is same
# create-drop -> deletes whole database and create every new table

